# 배틀 엔진 개발 및 롤백 보고서

## 1. 최근 작업 내역 (Backup 지점 이후)
- **배틀 엔진 고도화**: 랭크 변화, 상태이상, HP 회복/흡수 로직 구현.
- **이벤트 훅 시스템**: `onEntry`, `onTurnEnd` 등 트리거 시스템 도입.
- **UI 개선**: 배틀 중 실시간 랭크/상태 표시 및 기술 상세 정보 툴팁 추가.
- **커스텀 규칙 시스템(1차)**: 기술의 `effectTags`를 수정할 수 있는 GUI 및 저장 로직 구현.
- **엔진 리팩토링**: 하드코딩된 특성 로직을 제거하고 데이터(EffectTag) 주입 방식으로 전환 시도.
- **테스트 코드**: `battle-effects.test.ts`, `battle-hooks.test.ts` 등 검증 코드 추가.

## 2. 기존 의도 (User's Original Intent)
- **커스텀 최우선 아키텍처**: 모든 배틀 룰(능력치 계산, 기술 효과, 특성, 도구, 기믹 등)은 근본적으로 '커스텀 규칙' 시스템 위에서 동작해야 함.
- **프리셋 기반 기본팩**: "기본팩"은 그 자체로 특별한 로직이 아니라, 커스텀 시스템으로 만든 규칙들을 미리 모아놓은 "프리셋" 형태여야 함.
- **확장성**: 엔진은 특정 개체(예: 위협 특성)를 알 필요 없이, 주입된 데이터(규칙)를 실행하는 범용적인 '껍데기' 역할을 수행해야 함.

## 3. AI의 오해 및 문제점 (Disconnect)
- **단계별 접근의 오류**: AI는 "먼저 엔진을 코드로 짜고(하드코딩 포함), 나중에 커스텀 기능을 붙인다"는 식의 단계별 접근 방식을 취함.
- **근본 설계의 부재**: 사용자님은 **처음부터 모든 것이 커스텀 규칙 기반**이기를 원했으나, AI는 커스텀 기능을 배틀 엔진의 '일부 기능' 혹은 '나중에 추가할 고도화 작업'으로 치부함.
- **신뢰 저하**: "커스텀 기반"이라고 명시했음에도 불구하고 하드코딩된 샘플 로직을 먼저 제안하거나 구현하는 등, 설계 철학을 코드의 근간에 반영하지 못함.

## 4. 결정 및 조치
- **git 롤백**: 현재까지 진행된 "하이브리드형(코드+커스텀)" 코드를 폐기하고, 설계 철학이 왜곡되기 전의 상태로 돌아감.
- **재시작 방향**: 모든 데이터(종족값, 특성, 도구 포함)가 커스텀 규칙으로 처리될 수 있는 **"완전 데이터 주도형 아키텍처"**를 가장 먼저 설계하고, 그 위에 배틀 로직을 얹는 방식으로 진행함.

---
**이 보고서 작성 후, 현재 상태를 백업(commit)하고 명시된 지점으로 롤백을 수행합니다.**
