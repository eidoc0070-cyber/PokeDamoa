# Project Instructions: PokeDamora

## 핵심 설계 원칙 (Core Design Principles)
- **100% 데이터 주도형 배틀 엔진**: 모든 배틀 로직(능력치 계산, 기술 효과, 특성, 도구, 상태이상 등)은 근본적으로 데이터(JSON) 주입 방식으로 동작해야 합니다. 절대로 특정 개체나 규칙을 엔진 코드(`engine.ts` 등)에 하드코딩하지 마십시오.
- **커스텀 최우선 아키텍처**: 기본 데이터는 커스텀 시스템 위에서 동작하는 하나의 '프리셋'일 뿐입니다. 모든 기능은 사용자 정의가 가능하도록 설계되어야 합니다.
- **엔진의 역할**: 엔진은 특정 규칙을 아는 것이 아니라, 주입된 '효과 태그(Effect Tags)'를 정해진 타이밍(Hooks)에 실행하는 범용적인 실행기(Shell) 역할만 수행합니다.

## 개발 가이드라인 (Development Guidelines)
- 새로운 배틀 기믹을 추가할 때는 코드를 수정하기 전, 이를 표현할 수 있는 `EffectTag` 스키마와 데이터 구조를 먼저 고민하십시오.
- AI는 하드코딩의 유혹(단계별 접근을 빙자한 코드 우선주의)을 경계해야 하며, 항상 `ROLLBACK_REPORT.md`의 교훈을 상기하십시오.
