# 샌드백 AI 및 커스텀 배틀 시스템 고도화 계획 (Roadmap)

## 1. 개요
본 프로젝트의 샌드백 AI를 단순한 수치 계산기를 넘어, 사용자가 직접 규칙을 정의하고 실전과 동일한 변수를 테스트할 수 있는 **데이터 기반 배틀 엔진**으로 발전시키는 것을 목표로 합니다.

## 2. 현재 구현 현황 (Baseline & Phase 1 Done)
- [x] 실전 능력치 계산 공식 (종족값/IV/EV/성격/레벨)
- [x] 기본 데미지 계산 공식 및 16단계 난수 적용
- [x] 타입 상성 (단일/이중) 및 자속 보정(STAB)
- [x] 스피드 및 기술 우선도 기반 행동 순서 결정
- [x] 기절 시 즉시 교체 및 선출 단계(3vs3) UI
- [x] ID 기반의 최적화된 데이터 저장 구조 (Party/Moves/Abilities/Items)
- [x] **0단계 (구조 정리)**: 엔진 리팩토링 및 이벤트 훅 기반 마련 완료
- [x] **1단계 (데이터 보강)**: 한국어 상세 설명 및 구조화된 효과 태그(Effect Tags) 수집 완료

## 3. 데이터 고도화 및 커스텀 시스템
### A. 데이터 구조 확장 (`scripts/build-data.ts`)
- **한국어 상세 설명(Flavor Text)** 추가: 기술, 특성, 도구의 상세 효과 텍스트 수집 완료.
- **구조화된 효과 태그(Effect Tags) 단계적 도입**: 
    - **1순위 (단순 효과)**: 랭크 변화(`rank`), 위력 보정(`power_mod`), HP 회복(`heal`) 등 즉각적인 수치 변화. (데이터 확보 완료)
    - **2순위 (조건/확률)**: 특정 확률로 발동(`chance`), 상태이상 부여(`status`) 등 기초 확률 로직. (데이터 확보 완료)
    - **3순위 (복잡 조건)**: "HP 절반 이하", "특정 날씨/필드 시" 등 복잡한 조건부 로직 (심화 단계).

### B. 커스텀 규칙 시스템
- **통일된 규칙 엔진**: 기본 데이터와 사용자 정의(Custom) 데이터를 동일한 Effect Schema로 처리.
- **인터페이스 방향**: 일반 사용자도 쉽게 규칙을 만들 수 있도록 **GUI 기반의 구조화된 입력 폼(Form)** 제공. (내부적으론 JSON ID 기반 저장)

## 4. 배틀 엔진 고도화 계획
### 1단계: 변화기 및 단순 부가 효과 구현
- 랭크업 기술(칼춤 등) 및 단순 수치 보정 특성/도구 로직 추가.
- 공격 기술의 확률적 상태이상(마비, 화상 등) 반영.

### 2단계: 이벤트 훅(Event Hooks) 및 조건부 로직
- 배틀 흐름(공격 전/후, 턴 종료)에 개입하여 특성/도구 효과 처리.
- 복잡한 발동 조건(HP 제한, 타입 제한 등) 처리 로직 구축.

### 3단계: 세대별 특수 시스템 (Gimmicks) - 최후순위
- 모든 기초 배틀 로직 완성 후 통합 구현.
- 메가진화, Z기술, 다이맥스, **테라스탈** 시스템 및 관련 보정 로직 통합.

## 5. UI/UX 및 최적화 전략
### A. 정보 도감 강화
- 기술, 특성, 도구 리스트에 상세 설명 팝업/확장 영역 추가. (완료)
- 배틀 중에도 상대/내 포켓몬의 상세 상태(랭크, 특성 효과 등) 확인 가능하도록 개선.

### B. 데이터 최적화
- **필드 분리**: 배틀 엔진용 핵심 데이터와 UI용 텍스트 데이터를 분리하여 초기 로딩 속도 유지.
- **ID 참조 방식 고수**: 모든 내부 연산은 숫자(ID)로 수행하여 메모리 사용량 최소화.
- **압축**: JSON 데이터 배포 시 공백 제거 및 Gzip 압축 적용.

## 6. 향후 추진 순서 (Next Step: 2번)
1.  ~~**데이터 보강**: build-data.ts 수정 및 상세 설명 데이터 확보.~~ (완료)
2.  **엔진 1단계**: 랭크 변화 및 단순 상태이상 로직 구현. (확보된 `effectTags`를 엔진이 처리하도록 연동)
3.  **엔진 2단계**: 이벤트 훅 및 복잡 조건부 발동 시스템 구축. (커스텀 데이터 스키마 확정)
4.  **UI 개선 및 커스텀 인터페이스**: 
    *   도감 상세 설명 표시 (완료)
    *   **커스텀 규칙 관리 화면(폼 기반)** 구축 (반드시 엔진 2단계 완료 후 수행하여 데이터 규격 일치 보장)
5.  **기믹 구현**: 테라스탈, 메가진화 등 특수 시스템 통합.
